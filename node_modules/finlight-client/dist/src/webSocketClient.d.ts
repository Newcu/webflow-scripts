import { Article, BasicArticle, GetArticlesWebSocketParams } from './types';
import { ApiClientConfig } from './config';
export declare class WebSocketClient {
    private readonly config;
    private webSocket?;
    private pingInterval?;
    constructor(config: ApiClientConfig);
    connect(requestPayload: GetArticlesWebSocketParams & {
        extended: false;
    }, onMessage: (article: BasicArticle) => void): void;
    connect(requestPayload: GetArticlesWebSocketParams & {
        extended: true;
    }, onMessage: (article: Article) => void): void;
    private createPingInterval;
    private sendRequest;
    disconnect(): void;
    private receiveArticle;
    private handlePong;
}
